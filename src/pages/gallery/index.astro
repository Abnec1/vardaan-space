---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
import { SITE_TITLE } from '../../consts';

const gallery = (await getCollection('gallery')).sort(
	(a, b) => b.data.captureDate.valueOf() - a.data.captureDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title={`Gallery | ${SITE_TITLE}`}
			description="Astrophotography captures with short observing notes and setup details."
		/>
	</head>
	<body>
		<Header />
		<main>
			<section class="intro glass-card">
				<p class="chip">Payload Archive</p>
				<h1>Interstellar capture gallery</h1>
				<p>
					Each frame ships with mission context: targets, capture windows, and the equipment stack behind
					the result.
				</p>
			</section>

			<ul class="gallery-list">
				{
					gallery.map((entry) => (
						<li class="glass-card card">
							<a href={`/gallery/${entry.id}/`} class="card-link">
								<div class="image-wrap">
									<Image src={entry.data.image} alt={entry.data.title} width={1080} height={720} />
								</div>
								<div class="details">
									<p class="chip">{entry.data.captureDate.toLocaleDateString('en-US')}</p>
									<h2>{entry.data.title}</h2>
									<p>{entry.data.description}</p>
									<p class="meta">
										{entry.data.location && <span>{entry.data.location}</span>}
										{entry.data.equipment && <span>{entry.data.equipment}</span>}
									</p>
								</div>
							</a>
						</li>
					))
				}
			</ul>
		</main>
		<Footer />
	</body>
</html>

<style>
	.intro {
		padding: 1.3rem;
		margin-bottom: 1.2rem;
	}
	.gallery-list {
		list-style: none;
		padding: 0;
		display: grid;
		gap: 1.1rem;
	}
	.card {
		padding: 0.9rem;
	}
	.card-link {
		display: grid;
		grid-template-columns: 1.5fr 1fr;
		gap: 0.95rem;
		text-decoration: none;
		color: inherit;
	}
	.image-wrap img {
		width: 100%;
		aspect-ratio: 16 / 10;
		object-fit: cover;
	}
	.details h2 {
		margin-top: 0.7rem;
		margin-bottom: 0.4rem;
		font-size: 1.35rem;
	}
	.meta {
		display: flex;
		gap: 0.5rem;
		flex-wrap: wrap;
		margin: 0;
		font-size: 0.9rem;
		color: rgba(var(--muted), 0.95);
	}
	@media (max-width: 900px) {
		.card-link {
			grid-template-columns: 1fr;
		}
	}
</style>
